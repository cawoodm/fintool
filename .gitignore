# Node / Vite
node_modules/
dist/
.vite/

# Personal financial data — never commit (also covers a `data` symlink, not just a dir)
/data
data/

# Local scratch (API keys, etc.) — never commit
/tmp/
tmp/

# Screenshots show derived figures
screenshots/

# Local dev artifacts
server.log
*.log

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
