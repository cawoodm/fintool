{
  "name": "fintool",
  "private": true,
  "version": "0.7.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "publish": "pwsh ci/publish.ps1",
    "docker:build": "docker build -t fintool .",
    "docker:run": "docker run -d --restart always -p 8082:80 --name fintool fintool",
    "docker": "npm run docker:build && npm run docker:run && start http://localhost:8082"
  },
  "devDependencies": {
    "papaparse": "^5.4.1",
    "vite": "^5.4.0",
    "vitest": "^2.1.0"
  }
}